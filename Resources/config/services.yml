services:
    mattermost.helper:
        class: Crea\MattermostBundle\Services\MattermostHelper
        arguments:
            - %kernel.environment%

    mattermost_exception_logger.listener:
        class: Crea\MattermostBundle\Services\ExceptionHandler
        arguments:
            - @logger
            - %kernel.environment%
            - @mattermost.helper
        tags:
            - { name: kernel.event_listener, event: kernel.exception, method: onKernelException }